<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- Cleartext (plain http://) is only used for the manager's own optional, self-hosted
         Ollama endpoint (Settings > AI Shelf Count Setup); OpenAI/Anthropic calls are always
         https:// regardless of this setting. -->
    <application
        android:name=".OrderRadarApp"
        android:allowBackup="true"
        android:icon="@drawable/order_radar_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/order_radar_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.OrderRadar"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
